#!/usr/bin/bash

exec dbus-launch --exit-with-x11 &
exec ibus-daemon -drxR &

exec picom &

exec numlockx on &

if [[ $TERMCLT -eq 1 ]]; then
	exec i3
elif [[ $TERMCLT -eq 2 ]]; then
	exec startplasma-x11
elif [[ $TERMCLT -eq 3 ]]; then
	exec gnome-session
fi
